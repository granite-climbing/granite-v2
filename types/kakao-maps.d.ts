// Pulls in the global `kakao.maps` namespace declarations that
// react-kakao-maps-sdk expects as a peer type dependency. The package is not
// auto-included (tsconfig has no "types" array), so reference it explicitly.
/// <reference types="kakao.maps.d.ts" />
